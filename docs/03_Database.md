# Database

v0.1 intentionally has no database. Requests are stateless and prediction outputs are returned directly to the caller.

v0.2 will introduce prediction-history persistence behind a repository abstraction. Database selection and schema will be decided at that milestone.

