# CreditRisk

An illustrative sklearn `LogisticRegression` classifier. The `approved_prediction` result is `1` for low-risk / approved and `0` for high-risk / declined. Its deterministic artifact is produced by `backend/scripts/build_credit_risk_model.py`.

