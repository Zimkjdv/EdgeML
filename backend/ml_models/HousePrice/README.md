# HousePrice

An illustrative sklearn `LinearRegression` artifact built from deterministic sample data. It is included solely to make the v0.1 platform executable end to end.

Expected features are `Area`, `Room`, and `Age`. The model artifact is generated by `backend/scripts/build_example_model.py`; production artifacts are supplied by the deployment operator.

