# CustomerChurn

An illustrative sklearn `RandomForestClassifier`. The `churn_prediction` result is `1` when the customer is predicted to churn, otherwise `0`. Its deterministic artifact is produced by `backend/scripts/build_customer_churn_model.py`.

